# NetSuite Account Reconciliation Automation (SaaS-ready · 2025)

Zero-to-running in 30 seconds. Works with any NetSuite GL + Subledger CSV export.

## Quick Start
```bash
git clone https://github.com/yourname/netsuite-reconciliation-automation.git
cd netsuite-reconciliation-automation
python -m venv venv && source venv/bin/activate   # or conda
pip install -r requirements.txt
python src/reconcile.py
